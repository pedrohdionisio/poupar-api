# Cognito
COGNITO_CLIENT_ID: !Ref UserPoolClient
COGNITO_CLIENT_SECRET: !GetAtt  UserPoolClient.ClientSecret
COGNITO_POOL_ID: !Ref  UserPool

# Database
MAIN_TABLE_NAME: !Ref MainTable

# Storage
UPLOADS_BUCKET_NAME: !Ref UploadsBucket

# AI
GEMINI_API_KEY: ${ssm:/poupar/${self:provider.stage}/gemini-api-key, env:GEMINI_API_KEY}
GEMINI_MODEL: ${env:GEMINI_MODEL, 'gemini-3.6-flash'}
