listAccountProducts:
  handler: src/main/functions/accountProducts/listAccountProducts.handler
  events:
    - httpApi:
        path: /account-products
        method: GET
        authorizer:
          name: CognitoAuthorizer
